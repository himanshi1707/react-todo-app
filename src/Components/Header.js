import React from "react";

function Header() {
  return (
    <div
      style={{
        fontSize: "50px",
        fontWeight: "bolder",
        color: "#d47a25",
        display: "flex",
        justifyContent: "center",
        marginBottom: "1.5em",
      }}
    >
      TODO LIST
    </div>
  );
}

export default Header;
